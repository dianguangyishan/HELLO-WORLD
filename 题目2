1.  /dev/sdb7是拓展分区里的逻辑分区，拓展分区最多有一个。
2.  mbr开机引导程序和分区表信息，512b，（magic number 2b/partitionittable 64b/bootloader 446kb）
3.  可以在window环境下安装linux，也可以直接安装linux到一个无分区的地方，mbr是硬盘分区表格式，它最大的管理能力是1tb，大于1tb
要用gpt格式。
4.  设备bus信息，缺少一个模拟模块，可以到系统目录下去找。
5.  dev/null 是只写文件，写入它的会丢失，从里什么都读不了，对命令行和脚本非常有用。像/dev/null一样, /dev/zero也是一个伪文件,
但它实际上产生连续不断的null的流（二进制的零流，而不是ASCII型的）. 写入它的输出会丢失不见, 而从/dev/zero读出一连串的null也
比较困难, 虽然这也能通过od或一个十六进制编辑器来做到. /dev/zero主要的用处是用来创建一个指定长度用于初始化的空文件，就像临
时交换文件。
